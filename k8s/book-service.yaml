apiVersion: apps/v1
kind: Deployment
metadata:
  name: book-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: book-service
  template:
    metadata:
      labels:
        app: book-service
    spec:
      containers:
        - name: book-service
          image: us-central1-docker.pkg.dev/hello-481616/book-repo/book-service:latest
          ports:
            - containerPort: 8081
          resources:
            requests:
              cpu: "150m"
              memory: "300Mi"
            limits:
              cpu: "1000m"
              memory: "600Mi"
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            failureThreshold: 30
            periodSeconds: 10
          env:
            - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              value: "http://eureka-server:8761/eureka/"
---
apiVersion: v1
kind: Service
metadata:
  name: book-service
spec:
  selector:
    app: book-service
  ports:
    - port: 8081
      targetPort: 8081
  type: ClusterIP